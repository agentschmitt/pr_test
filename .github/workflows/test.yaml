name: test
on:
  create:
  delete:
  workflow_dispatch:
jobs:
  trigger:
    name: trigger
    runs-on: ubuntu-latest
    if: ${{ github.event.ref_type != 'tag' }}
    steps:
      - run: echo "${{ github.event.ref_type }}"
      - run: echo "${{ github.ref }}"
